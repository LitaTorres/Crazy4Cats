 <div class="reglas"> 
    <%= form_with(model: publication) do |form| %>
      <% if publication.errors.any? %>
        <div style="color: red">
          <h2><%= pluralize(publication.errors.count, "error") %> prohibited this publication from being saved:</h2>

          <ul>
            <% publication.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div>
        <%= form.label :title, "Título de tu publicación", style: "display: block" %>
        <%= form.text_field :title %>
      </div>

      <div>
        <%= form.label :image, "Link de tu imagen", style: "display: block" %>
        <%= form.text_field :image %>
      </div>

      <div>
        <%= form.label :description, "Descripción" ,style: "display: block" %>
        <%= form.text_area :description %>
      </div>

      <div>
        <%= form.hidden_field :user_id, value: current_user.id %>
      </div>

      <div>
        <%= form.submit %>
      </div>
    <% end %>
</div>  